#solicirar notas al usuarop

lista_notas = []

cantidad_notas = float(input("ingresa la cantidad de nota"))

contador = 0
while(contador < canridad_notas):
    notas = float(input("ingrese la notas"))
    lista_notas.append(nota)
    contador+=1

   for cada_elemento in lista_notas :
       suma = suma+lista_notas[cada_elemento]

promedio = notas/cantidad_notas
print(promedio)
       
